{
  "name": "starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "bankai start index.js",
    "build": "bankai build index.js",
    "deploy": "rm -rf dist && npm run build && cp CNAME dist && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "morphable": "^0.7.0",
    "nanohtml": "^1.2.4",
    "sheetify": "^7.3.3",
    "tachyons": "^4.11.1",
    "viewroute": "^0.2.0",
    "xoo": "^1.2.1"
  },
  "devDependencies": {
    "bankai": "^9.15.3",
    "gh-pages": "^1.2.0"
  }
}
